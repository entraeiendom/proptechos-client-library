package io.entraos.idun.rec.sensor;

import io.entraos.idun.commands.IdunGetCommand;

public class GetSensorCommand extends IdunGetCommand {
    protected GetSensorCommand(String groupKey) {
        super(groupKey);
    }
}
